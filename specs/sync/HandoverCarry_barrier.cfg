\* The shipped handover: the grid.sync at the prefill -> decode seam. Every
\* block completes its prefill writes before any block reads decode state.
\* EXPECTED: no error; the fused kernel is both safe (NoPrematureConsume) and
\* live (Progress).
SPECIFICATION Spec
CONSTANTS
    Blocks = {b1, b2}
    HandoverBarrier = TRUE
INVARIANTS
    TypeOK
    NoPrematureConsume
PROPERTIES
    Progress
