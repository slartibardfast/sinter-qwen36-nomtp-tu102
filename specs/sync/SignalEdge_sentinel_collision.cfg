\* Sentinel minus domain separation: the datum equals the sentinel value.
\* EXPECTED: TLC MUST report the Progress property violated (livelock:
\* the poll cannot distinguish data from reset fill).
SPECIFICATION Spec
CONSTANTS
    Scheme = "sentinel"
    ProducerFence = FALSE
    StrongReads = TRUE
    StoresOrdered = TRUE
    SentinelCollision = TRUE
INVARIANTS
    TypeOK
    NoStaleConsume
PROPERTIES
    Progress
