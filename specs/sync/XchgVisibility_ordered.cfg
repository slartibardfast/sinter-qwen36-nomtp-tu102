\* The shipped XCHG protocol: sys-fenced push (membar.sys + boundary before the
\* seqno release) AND strong .cg consume. EXPECTED: no error; the exchange is
\* both safe (NoStaleConsume) and live (Progress).
SPECIFICATION Spec
CONSTANTS
    PushFenced = TRUE
    StrongConsume = TRUE
INVARIANTS
    TypeOK
    NoStaleConsume
PROPERTIES
    Progress
