\* E1 dropped: no __threadfence_block, so the ready-flag store may pass the
\* tile stores. The WAR guard is left ON to isolate the fence edge.
\* EXPECTED: TLC MUST report NoStaleTile violated -- compute polls a raised
\* flag and reads a tile the mover has not finished writing (gData still STALE
\* or an older round). A clean pass means the model lost the visibility hazard.
SPECIFICATION Spec
CONSTANTS
    ProducerFence = FALSE
    WARGuard = TRUE
INVARIANTS
    TypeOK
    NoStaleTile
