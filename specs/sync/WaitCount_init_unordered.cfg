\* A notify may race the init write (the hazard ETC never acknowledges).
\* EXPECTED: TLC MUST report ProceedSound violated (stale 1: early release)
\* and/or Progress violated (stale 0: lost decrement, the count never zeroes).
SPECIFICATION Spec
CONSTANTS
    Producers = {p1, p2, p3}
    InitOrdered = FALSE
INVARIANTS
    TypeOK
    ProceedSound
PROPERTIES
    Progress
