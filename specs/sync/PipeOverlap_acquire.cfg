\* The shipped pipe-overlap protocol: producer release fence AND consumer
\* acquire (the payload read ordered after the flag poll). EXPECTED: no error;
\* the pipe is both safe (NoStaleConsume) and live (Progress).
SPECIFICATION Spec
CONSTANTS
    ProducerRelease = TRUE
    ConsumerAcquire = TRUE
INVARIANTS
    TypeOK
    NoStaleConsume
PROPERTIES
    Progress
