\* Sentinel with the full adoption trio: strong polls, ordered stores,
\* domain-separated sentinel. SAFE - this is what adopting would require.
SPECIFICATION Spec
CONSTANTS
    Scheme = "sentinel"
    ProducerFence = FALSE
    StrongReads = TRUE
    StoresOrdered = TRUE
    SentinelCollision = FALSE
INVARIANTS
    TypeOK
    NoStaleConsume
PROPERTIES
    Progress
