\* E3 MUST-FAIL: a U-tile exchange where the reducer folds MORE per-token
\* slices than the pusher landed (FoldWidth 2 > PushWidth 1) under PERFECT
\* E1/E2 ordering. The excess slice folds the inbox's previous-pass bytes:
\* NoStaleConsume must be violated. This pins the host's single-sourced
\* n_tok width coupling (dual_run_pass stages one width to both GPUs) as
\* load-bearing, not incidental.
SPECIFICATION Spec
CONSTANTS
    PushFenced = TRUE
    StrongConsume = TRUE
    PushWidth = 1
    FoldWidth = 2
INVARIANTS
    TypeOK
    NoStaleConsume
