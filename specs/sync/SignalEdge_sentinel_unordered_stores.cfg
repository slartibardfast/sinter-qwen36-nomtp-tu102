\* Sentinel minus the store-order leg (ordering is observed on this rig,
\* guaranteed nowhere). EXPECTED: TLC MUST report NoStaleConsume violated.
SPECIFICATION Spec
CONSTANTS
    Scheme = "sentinel"
    ProducerFence = FALSE
    StrongReads = TRUE
    StoresOrdered = FALSE
    SentinelCollision = FALSE
INVARIANTS
    TypeOK
    NoStaleConsume
PROPERTIES
    Progress
