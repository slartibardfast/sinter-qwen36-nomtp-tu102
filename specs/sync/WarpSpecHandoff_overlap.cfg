\* The shipped overlap protocol: both ordering edges enforced --
\* a __threadfence_block before the ready-flag (E1) AND a consumed-flag
\* WAR guard before buffer reuse (E2). EXPECTED: no error; the pipeline
\* is both safe (NoStaleTile) and live (Progress).
SPECIFICATION Spec
CONSTANTS
    ProducerFence = TRUE
    WARGuard = TRUE
INVARIANTS
    TypeOK
    NoStaleTile
PROPERTIES
    Progress
