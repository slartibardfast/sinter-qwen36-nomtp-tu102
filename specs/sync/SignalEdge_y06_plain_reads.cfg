\* The measured 999999/1e6 hazard: fenced producer, PLAIN consumer reads.
\* EXPECTED: TLC MUST report NoStaleConsume violated (stale body via cache).
\* A clean pass means the model lost the hazard.
SPECIFICATION Spec
CONSTANTS
    Scheme = "flag"
    ProducerFence = TRUE
    StrongReads = FALSE
    StoresOrdered = FALSE
    SentinelCollision = FALSE
INVARIANTS
    TypeOK
    NoStaleConsume
PROPERTIES
    Progress
