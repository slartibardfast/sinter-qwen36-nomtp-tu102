\* The MTP-off instantiation (the worked example): no speculation, no drafter.
\* The playbook's degeneracy obligation: the SAME module, k = 0.
SPECIFICATION Spec
CONSTANTS
    SpecDepth = 0
    HasDrafter = FALSE
    Workers = {w1, w2}
CONSTRAINT PassBound
INVARIANTS
    TypeOK
    NoStaleRead
    BankIsolation
    BaseIntegrity
    DegenerateAtZero
PROPERTIES
    Progress
